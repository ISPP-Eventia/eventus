import React from "react";

import Page from "../page";

export type EventUs = {
  image?: string;
  title?: string;
  description?: string;
  price?: number;
  date?: string;
};

const EventDetailPage = () => {
  // TODO
  return <Page title="DETAIL PAGE TODO"></Page>;
};

export default EventDetailPage;
